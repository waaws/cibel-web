# cibel-web
Website de Cibel

Para correr en Dev:

- La primera vez:
  ```
  npm install
  ```

Para levantar el servidor:

  ```
  sudo npm install -g webpack
  webpack-dev-server
  ```

Para acceder a la web:

  ```
  localhost:8080
  ```
